class Solution {
    public boolean rotateString(String s, String goal) {
        if(s.length()!=goal.length())
        return false;
        String t=goal+goal;
        if(t.contains(s))
        return true;
        return false;
    }
}
